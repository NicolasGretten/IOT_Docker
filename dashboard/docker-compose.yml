version: '3'
services:
  react:
    container_name: dashboard
    build:
      context: .
      args:
        - GITLAB_BRANCH=${CI_COMMIT_BRANCH}
      dockerfile: dockerfile/react/dockerfile
    image: 'collectandverything-dashboard:latest'
    restart: unless-stopped
    tty: true
    networks:
      - collectandverything
networks:
  collectandverything:
    external: true
